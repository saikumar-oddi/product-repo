## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/products_service?serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = Rgukt123@
server.port=8000
## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

#spring.jpa.open-in-view=false
# Below property is to configure context path for spring boot app. 
# If you don't want context path then comment below property
#server.servlet.context-path=/springboot-crud-rest
